import { useTranslation, Trans } from 'react-i18next'

const User  = (props) => {
    const { t } = useTranslation();

    return (
        <div>
            user component
        </div>
    )
}

export default User;